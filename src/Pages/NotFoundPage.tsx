import { Typography } from "@mui/material";

const NotFoundPage = () => {
  return (
    <Typography variant="h3" sx={{ mt: 20 }}>
      404 - Nepavyko surasti ko ieškojote
    </Typography>
  );
};

export default NotFoundPage;
