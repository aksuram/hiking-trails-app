import { Typography } from "@mui/material";

const NotFound = () => {
  return (
    <Typography variant="h3" sx={{ mt: 20 }}>
      404 - Nerasta ko ieškojote
    </Typography>
  );
};

export default NotFound;
